import React from 'react'
import { SafeAreaView, Text } from 'react-native'

const LoadingScreen = () => (
  <SafeAreaView>
    <Text>Loading...</Text>
  </SafeAreaView>
)

export default LoadingScreen
